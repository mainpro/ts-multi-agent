# 详细设计文档

> **版本**: v1.0  
> **配套**: `PRD.md`(产品需求),`分析报告.md`(风险/优化)  
> **范围**: 描述 `ts-multi-agent` 当前实现的技术架构与设计要点

---

## 1. 文档目的

本文档描述 `ts-multi-agent` 的**详细设计**:
- 进程内模块划分与依赖关系
- 核心数据流(从 HTTP 请求到响应)
- 关键算法(DAG 调度 / 合并 / 断点续传 / Steer)
- 数据契约(类型 / 状态机 / 持久化)
- 安全/可观测性/部署细节

读者: 工程师。阅读本文档后应能在不阅读所有源代码的前提下:
- 理解各模块职责边界
- 知道新增业务应改哪个文件
- 理解关键设计权衡

---

## 2. 技术栈与运行时

| 项 | 选型 | 理由 |
|---|---|---|
| 语言 | TypeScript 5.3 (strict) | 编译期类型安全 |
| 运行时 | Node.js 20+ / Bun (dev/test) | 性能 + 内置 TS |
| HTTP | Express 5 + cors + rate-limit | 成熟生态,SSE 友好 |
| 数据校验 | Zod 3 | 与 TS 类型同步,运行时校验 |
| LLM | SiliconFlow + Haier(双 provider) | 国内可用 |
| 构建 | tsup → 单文件 dist/index.js | 部署无需源码 |
| 容器 | Docker + docker-compose | 标准化交付 |

`tsconfig.json` 全 strict,启用 `noUnusedLocals` / `noUnusedParameters` / `noImplicitReturns` 等约束。

---

## 3. 进程架构

### 3.1 单进程角色

**单进程 = 一个数字员工**。进程身份由 `employees/<id>.json` 决定。
- 启动时通过 `--employee=<id>` 选定,或 fallback 到目录第一个 enabled
- 进程内持有 1 个 MainAgent + 1 个 SubAgent + 1 套记忆 + 1 个 SkillRegistry
- 多实例部署 = 起多个进程,每个挂不同 JSON

### 3.2 Bootstrap 流程

`src/index.ts:70-205`:

```
1. dotenv.config() 加载 .env
2. buildFallbackLLMClient() 创建 LLM 客户端(fallback 链)
3. SkillRegistry().scanSkills() + startWatch() 扫描技能
4. MemoryService(DATA_DIR, llm) 创建 4 层记忆
5. SubAgent(skillRegistry, llm, memory) 单纯执行器(无 persona)
6. TaskQueue(executor) 创建任务队列
7. MainAgent 依赖注入:IntentRouter / UserProfileService / DynamicContextBuilder /
   SessionStore / AskAgent / SystemSkillLoader / ExecutorRegistry
8. loadEmployeeConfig() 读 employees/<id>.json
9. MainAgent(deps, { employee }) 创建主智能体(持有员工身份)
10. createAPIServer() Express 应用,监听 PORT
```

异常处理: 任何步骤失败抛 `BootstrapError`,由 `globalErrorHandler` 统一转为 500。

### 3.3 进程内模块依赖图

```
                    ┌──────────────┐
                    │  HTTP Layer  │
                    │ (Express API)│
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  MainAgent   │ ←── 持有 EmployeeConfig
                    │ (orchestrator)│
                    └──┬───┬───┬───┘
        ┌──────────────┘   │   └──────────────┐
        │                  │                  │
   ┌────▼─────┐    ┌───────▼──────┐    ┌──────▼──────┐
   │IntentRouter│   │UnifiedPlanner│   │  AskAgent   │
   └────┬──────┘    └──────┬───────┘    └──────┬──────┘
        │                  │                    │
        └────────┬─────────┘                    │
                 │                              │
        ┌────────▼──────────┐          ┌────────▼────────┐
        │ TaskGraphExecutor │          │   SessionGate   │
        └────────┬──────────┘          └────────┬────────┘
                 │                              │
        ┌────────▼──────────┐          ┌────────▼────────┐
        │    TaskQueue      │          │  SessionStore   │
        └────────┬──────────┘          └─────────────────┘
                 │
        ┌────────▼──────────┐
        │     SubAgent      │ ←── 读 task._personaContext / task.allowedTools
        │  (pure worker)    │
        └────────┬──────────┘
                 │
   ┌─────┬───────┼───────┬─────┬─────┐
   ▼     ▼       ▼       ▼     ▼     ▼
 read  write  edit   bash  glob  grep  ask_user  conversation-get  append_improvement
                             │
                       PathGuard (路径 + 命令白名单)
                             │
                       Bubblewrap Sandbox (若可用)

横切依赖:
  LLMClient ──── FallbackLLMClient ──── LLM providers
  MemoryService ──── L1/L2/L3/L4 storage
  Observability ──── Logger / Metrics / SLA / Attribution
```

---

## 4. 核心数据流

### 4.1 端到端流程:`POST /tasks/stream` 单任务场景

```
[1] HTTP API 入口                          src/api/index.ts:344-670
    ├─ 加载 userProfile (req.profile)      src/api/index.ts:352-362
    ├─ guardrail middleware (deny/rewrite) src/api/index.ts:364
    ├─ 订阅 lifecycle / task / reasoning   src/api/index.ts:462-511
    ├─ mainAgent.gateCheck()               src/api/index.ts:518-521
    │   ├─ 若 queueFull → 503 JSON
    │   ├─ 若 queued → 202 JSON
    │   └─ proceed → flushHeaders + 写 SSE start
    └─ mainAgent.processRequirement()      src/api/index.ts:602-605
        │
[2] processRequirement                     src/agents/main-agent.ts:219-251
    ├─ SLA Tracker 启动(placeholder ID)
    ├─ gate.decide() (若 gateChecked=false)
    ├─ saveUserMessage → L1 + L4
    ├─ 调用 _processRequirementInner       src/agents/main-agent.ts:258-439
        │
[3] AskAgent.handleUserInput              src/agents/ask-agent.ts:47-104
    ├─ 检查 waitingRequest
    │   ├─ 是 continuation → 路径 [6a]
    │   └─ 否 → suspendRequest + 清 pending tasks
    └─ createRequest (新 Request)
        │
[4] processNormalRequirement               src/agents/main-agent.ts:903-1430
    ├─ 并行加载:userProfile / memory / session / dynamicContext
    ├─ 召回相关记忆(L3 embedding)
    ├─ 注入 session / 动态上下文到 enrichedRequirement
    │
    ├─ IntentRouter.classify()             src/routers/intent-router.ts:73-118
    │   ├─ buildSystemPrompt(persona, displayName)  ← employee.persona
    │   ├─ llm.generateStructured(IntentSchema)
    │   └─ 返回 {intent, tasks[], question?}
    │
    ├─ 若 intent != skill_task → handleNonSkillIntent
    │   (small_talk / confirm_system / out_of_scope / unclear)
    │
    ├─ skill 白名单校验(派单前)           src/agents/main-agent.ts:1137-1150
    │
    ├─ tasks 数 = 1 → 直接构造 TaskPlan
    │       tasks 数 > 1 → UnifiedPlanner.plan()    src/planners/unified-planner.ts:77-171
    │       └─ llm.generateStructured(UnifiedPlanSchema)
    │
    ├─ computeAllowedTools(skill, employee) src/agents/employee/tools.ts:32-57
    │   └─ skill ∩ employee.enabled − employee.denied
    │
    ├─ 构造 TaskGraph + 透传 persona/allowedTools 到运行时 Task
    │
    ├─ executeTaskGraph()                   src/agents/task-graph-executor.ts:389-496
    │   ├─ buildTaskGraph (topological sort + 分层)
    │   ├─ executeLayers
    │   │   ├─ 对每层 task: addTask → SubAgent.execute
    │   │   ├─ onCheckpoint (per layer, after all tasks complete)
    │   │   │   ├─ 检查 pending requests → drain → spawnMergedRequest(R2)
    │   │   │   └─ 若 spawn 了 → 返回 {shouldStop:true}, 中断 R1
    │   │   └─ 失败 / waiting → 收集 failedTasks / waitingTaskId
    │   └─ 完成后回写 task 状态到 session
    │
    └─ ResultAggregator.summarizeResults() (多任务时) src/agents/result-aggregator.ts:187-286
        ├─ llm 判断 completed + 生成 summary
        ├─ 应用 resultRewriter (append 'completed' + != 'waiting_user_input')
        └─ saveAssistantMessage → L1 + L4 + session.completeRequest
                                    │
[5] SubAgent.execute (worker)               src/agents/sub-agent.ts:167-254
    ├─ loadFullSkill(skillName)             ← SKILL.md body
    ├─ executeSkill()
    │   ├─ 拼 persona prefix 到 skill body
    │   ├─ buildSubAgentPrompt (skill + tools + history + profile + recalled memory)
    │   ├─ 工具过滤:task.allowedTools ?? skill.allowedTools ?? DEFAULT_SAFE_TOOLS
    │   ├─ llm.generateWithTools(messages, tools, toolExecutor, ...)
    │   │   ├─ 每轮前 consumeSteering() → push user message
    │   │   ├─ 解析 tool_calls
    │   │   ├─ 拆分 safe (并行) / unsafe (串行)
    │   │   ├─ toolExecutor:
    │   │   │   ├─ 未知工具熔断(UnknownToolLoopGuard, >3 触发)
    │   │   │   ├─ hooks emit (BEFORE/AFTER_TOOL_CALL)
    │   │   │   ├─ toolRegistry.execute(toolName, args, ctx)
    │   │   │   └─ bash code != 200 → throw
    │   │   └─ 重复直到 LLM 不再调工具(content-only)
    │   │
    │   ├─ 检测 ask_user 工具(必须是最后一次 tool call)
    │   ├─ 检测文本中的提问意图(detectQuestion)
    │   └─ 返回 SkillExecutionResult
    │
    └─ 保存 conversationContext + completedToolCalls 到 task(供断点续传)
                                    │
[6] 响应路径                                   │
    ├─ SSE 流式:每轮 reasoning 实时 emit     │
    ├─ 最终 complete event 携带 result       │
    └─ API finally cleanup: off 订阅 / end res │
```

### 4.2 关键路径:`ask_user` 挂起 → 续传

```
[6a] continueRequest 路径                  src/agents/main-agent.ts:554-676
    ├─ 找到关联 task
    ├─ 若 task 在 TaskQueue 中
    │   ├─ 自动填充 params[paramName] = answer
    │   ├─ 追加 questionHistory
    │   ├─ params.latestUserAnswer = answer
    │   ├─ params.conversationSummary = 历史问答
    │   ├─ 重置 task.status = pending / result = undefined
    │   └─ taskQueue.triggerProcess() → pollTaskCompletion
    │
    └─ 若 task 不在 TaskQueue(进程重启后)
        ├─ reconstructTask() 重建 Task 对象 + questionHistory
        ├─ 自动填 paramName + conversationSummary
        └─ addTask + pollTaskCompletion
```

`pollTaskCompletion` 内部用 `onceTaskEvent` 监听 `task-completed / task-failed`,带超时兜底。

### 4.3 关键路径:Checkpoint 让位 + R2 合并

```
触发: R1 任务图某层完成后,onCheckpoint 被调用

onTaskGraphCheckpoint:                    src/agents/main-agent.ts:703-764
    ├─ 读 session, 检查 pendingRequests
    ├─ 若 pending 为空 → return undefined (R1 继续)
    ├─ 否则:
    │   ├─ 标记 currentRequest.status = 'checkpoint_reached'
    │   ├─ 清 session.activeRequestId
    │   ├─ 清 R1 未启动的 task(TaskQueue.removePendingTask)
    │   ├─ 保留已完成 task(completedTaskIds 不动)
    │   ├─ emit request_checkpoint event(SSE 通知前端)
    │   └─ spawnMergedRequest()           src/agents/main-agent.ts:770-832
    │       ├─ gate.drain() 取出 pending
    │       ├─ parent.content + '\n---\n' + drained.requirements
    │       ├─ 创建新 Request R2
    │       ├─ emit request_spawned event
    │       └─ processRequirement(R2, {skipGate, requestOverride})
    │           └─ handleMergedCompletion (成功 / 失败)
    └─ return {shouldStop: true}           ← executeLayers 收到后停止 R1 后续 layer
```

### 4.4 关键路径:Steer 注入

```
触发: session 有 activeRequest 且至少 1 个 task.status === 'running'

[API 层] /tasks/stream
    ├─ mainAgent.shouldSteer(userId, sessionId)
    │   └─ 检查 activeRequest.status === 'processing' + 有 running task
    ├─ 若 true → steeringBuffer.enqueue({content, enqueuedAt})
    └─ 返回 202 {steered: true}

[SubAgent 工具循环] 每轮 LLM 调用前
    ├─ consumeSteering(sessionId) → messages.push({role: user, content})
    └─ emit request_steered event (SSE 通知前端)
```

**关键**: 仅在 active request 有 running task 时 steer,否则回退到 gate 队列(steer 无人消费会丢失)。

---

## 5. 数据契约

### 5.1 核心类型(摘选)

`src/types/index.ts` 是类型中枢:

```typescript
// Task 是运行时任务对象
interface Task {
  id: string;
  requirement: string;
  skillName?: string;
  status?: 'pending' | 'running' | 'completed' | 'failed' | 'suspended' | 'waiting';
  result?: TaskResult;
  error?: TaskError;
  params?: Record<string, unknown>;
  dependencies: string[];
  dependents?: string[];
  sessionId?: string;
  userId?: string;
  conversationContext?: Message[];  // LLM 上下文(断点续传)
  completedToolCalls?: CompletedToolCall[];
  executor?: TaskExecutor;          // per-task 绑定(P3 race fix)
  _personaContext?: PersonaContext; // Master 注入的 persona
  allowedTools?: string[];          // Master 注入的工具列表
}

// TaskPlan 是 LLM 规划输出
interface TaskPlan {
  id: string;
  requirement: string;
  tasks: Array<{
    id: string;
    requirement: string;
    skillName: string;
    params?: Record<string, unknown>;
    dependencies: string[];
    _personaContext?: PersonaContext; // 由 MainAgent 注入(派单前)
    allowedTools?: string[];          // 由 MainAgent 注入(派单前)
  }>;
}

// TaskGraph 是拓扑排序后的 DAG
interface TaskGraph {
  id: string;
  requirement: string;
  nodes: TaskGraphNode[];
  layers: string[][];  // layers[i] = 第 i 层可并行执行的任务
}

// Request 是 session 内的一个用户请求
interface Request {
  requestId: string;
  content: string;
  status: RequestStatus;
  tasks: RequestTask[];          // 持久化的 task 副本
  questions: QAEntry[];          // 主智能体问题
  result: string | null;
  executionProgress?: ExecutionProgress; // 断点续传用
}

// Session 是用户级会话
interface Session {
  userId: string;
  sessionId: string;
  requests: Request[];
  activeRequestId: string | null;
  pendingRequests: PendingRequest[];  // checkpoint 合并队列
}
```

### 5.2 EmployeeConfig 结构

`src/agents/employee/json-types.ts`:

```typescript
{
  employee: { id, displayName, enabled },        // 身份
  persona: { prefix, style?, boundaries? },     // 人设
  capabilities: {
    skillWhitelist: { type: 'allowlist', skills[] } | { type: 'unrestricted' },
    tools: { enabled[], denied[] },              // 3 段过滤
    llm: { provider, fallbackProvider?, temperature?, maxTokens? },
  },
  planning: { maxParallelTasks?, decompositionHint? },  // 偏好
  outputBehavior: { resultRewriter?: { match?, transform, value } }
}
```

### 5.3 状态机

#### Request 状态机
```
[New] → processing ──┬─→ waiting (ask_user 挂起)
                      ├─→ completed (所有 task 完成 + 汇总完成)
                      ├─→ failed (致命错误)
                      └─→ checkpoint_reached (R1 让位给 R2)

waiting → processing (用户回答问题)
completed/failed → 终态
```

#### Task 状态机
```
[New] → pending → running → completed
                   ├─→ failed (skill 抛错 / 超时)
                   ├─→ waiting (ask_user 挂起) → pending (用户回答)
                   └─→ suspended (会话挂起) → pending (会话召回)
```

---

## 6. 持久化设计

### 6.1 文件布局

```
data/
└── memory/
    ├── {userId}/
    │   ├── profile.json                  # L2 用户档案
    │   ├── session/{sessionId}.json      # SessionStore(状态机)
    │   ├── history/{sessionId}.json      # L4 完整历史
    │   └── summaries/{sessionId}.json    # L3 请求摘要
```

### 6.2 SessionStore 写入策略

- **默认防抖 100ms** 写盘(`saveSession`)
- **特殊情况立即刷盘**(`flushToDisk`): waiting 状态、pending queue 入队/出队、executionProgress 完成态
- **启动清理**: `cleanupStaleSessions()` 扫所有 session,把 dangling / processing >0 的 active request 修正
- **内部字段过滤**: `stripInternalFields` 当前是 identity(确保 `pendingRequests` 等关键字段不被丢弃)

### 6.3 4 层记忆

| 层 | 存储 | TTL | 内容 | 用途 |
|---|---|---|---|---|
| **L1** | 内存 Map | 30min | conversation[] + pendingRequestSummary | 会话级快速上下文 |
| **L2** | JSON 文件 | 永久 | profile + preferences + history | 跨会话用户画像 |
| **L3** | JSON + embedding | 30d | requestSummaries + sessionSummaries | 语义召回 |
| **L4** | JSON 文件 | 永久 | 完整 history entries | 进程重启后恢复 |

**写一致性**:
- L1 + L4 **同步双写**(`saveUserMessage` / `saveAssistantMessage`)
- L3 摘要**异步生成**,失败不阻塞主流程(`summarizeRequest` 包 fire-and-forget)
- L2 增量更新(`updateProfile`)

---

## 7. 关键算法

### 7.1 DAG 拓扑分层(`TaskGraphExecutor.buildTaskGraph`)

```typescript
1. 对每个 node:
   - inDegree[taskId] = dependencies.length
   - dependents[taskId] = []

2. 拓扑排序:
   while processed.size < nodes.length:
     ready = nodes.filter(n => !processed && inDegree[n] === 0)
     if ready.empty: 剩余节点放入同一层(检测到环的兜底)
     layers.push(ready)
     for n in ready:
       processed.add(n)
       for d in dependents[n]:
         inDegree[d]--

3. 返回 {layers: string[][]}
```

**复杂度**: O(V + E),V = tasks, E = dependencies。

### 7.2 工具 3 段过滤(`computeAllowedTools`)

```typescript
1. baseSet = skill.allowedTools ?? DEFAULT_SAFE_TOOLS
2. if employee.enabled:
     baseSet = baseSet ∩ enabled
3. if employee.denied:
     baseSet = baseSet − denied
4. 返回 baseSet (Set<string>)
```

**关键差异**:
- `taskAllowedTools !== undefined` 优先于 skill.allowedTools(Task 7 安全 fix: 避免 `[]` 被当成 "未设置")
- 员工黑名单**最高优先级**(即使 enabled 通过也会被剔除)

### 7.3 Pending 队列合并(`SessionGate + spawnMergedRequest`)

```typescript
触发: onCheckpoint 检测到 session.pendingRequests.length > 0

合并算法:
  mergedContent = parent.content
  for p in drain():
    mergedContent += '\n---\n\n' + p.requirement

限制: MAX_PENDING_REQUESTS = 10(LLM 上下文保护)
```

### 7.4 工具调用拆分(`LLMClient.generateWithTools`)

```typescript
每轮 LLM 调用后:
  safeCalls = tool_calls.filter(c => concurrencyChecker(c.name, c.args))
  unsafeCalls = tool_calls.filter(c => !concurrencyChecker(c.name, c.args))

  safeCalls → Promise.allSettled (并行)
  unsafeCalls → for-await (串行)

  concurrencyChecker 规则: DEFAULT_SAFE_TOOLS.has(name)
  // safe: read / glob / grep / conversation-get / ask_user / append_improvement
  // unsafe: write / edit / bash
```

### 7.5 CONTEXT_TOO_LONG 压缩(`SubAgent.executeSkill`)

```typescript
while attempts <= 1:
  try:
    return llm.generateWithTools(messages, ...)
  except LLMError('CONTEXT_TOO_LONG') and attempts < 1:
    messages = compactMessages(messages, llm, {tokenBudget: 8000, keepRecent: 5})
    attempts++

// 不可达:循环要么 return 要么 throw
```

`compactMessages` 通过 LLM 总结早期 tool_result + 保留最近 5 条消息,把 token 数压到 8000 以内。

---

## 8. 安全设计

### 8.1 三层防御

| 层 | 实现 | 作用 |
|---|---|---|
| **路径白名单** | `PathGuard.checkPath` 解析 realpath,校验必须在 workDir 内 | 防止 read/write 越权 |
| **路径黑名单** | regex 拦截 .ssh / .aws / .env / .pem / .key 等 | 防止访问凭据 |
| **Bash 命令白名单** | 只允许已知前缀(node scripts/, npm, ls 等) | 防止 shell 注入 |
| **Bash 危险模式黑名单** | sudo / eval / `...` / $(...) / rm -rf / 等兜底 | 防止命令替换 |

### 8.2 Guardrail 中间件

`src/guardrail/`:
- 关键词规则 + PII 检测(`pii-patterns.ts` 识别身份证/手机/邮箱)
- 用户角色 + 权限矩阵(`role: 'employee'` 默认无 privileged)
- 三档决策:**deny** (拒绝) / **rewrite** (改写) / **alert** (放行但审计)

### 8.3 错误分类(`errors/`)

```
AppError (基类,带 type/code/statusCode/cause)
├── LlmError (LLM 调用错误,继承 LLMError type)
├── SkillError (技能执行错误)
├── BusinessError (业务规则错误)
├── ConfigError (配置错误)
└── BootstrapError (启动错误)
```

所有 AppError 在 TaskGraphExecutor 保留 `originalError`,最终由 globalErrorHandler 转为 HTTP 响应,type/code 不丢失。

---

## 9. 可观测性设计

### 9.1 结构化日志

所有模块通过 `createLogger({module})` 拿到 logger,日志格式:
```json
{"timestamp":"2026-08-14T...","level":"info","module":"MainAgent","message":"收到用户请求","requirement":"..."}
```

关键事件埋点:
- `llm.request` / `llm.response`: LLM 调用边界
- `tool.call` / `tool.result` / `tool.exception`: 工具调用
- `task-started` / `task-completed` / `task-failed`: 任务事件(翻译为 SSE `task_*`)
- `request_queued` / `request_checkpoint` / `request_spawned`: request 生命周期
- `request_steered`: steer 注入

### 9.2 Metrics + SLA + Attribution

| 维度 | 计数器 / 计时器 |
|---|---|
| LLM | `llm.calls`、`llm.latency`、`llm.errors{kind}` |
| Skill | `skill.calls`、`skill.latency`、`skill.errors` |
| SLA | `sla.breached{type: GENERIC/LLM/SKILL/REQUEST/STEER_DRAIN}` |
| Guardrail | `guardrail.alerts{action}` |
| Attribution | `attributionCounter.{kind: SLA_BREACH, type}` |

`/metrics` 端点暴露 task metrics(完成/失败/超时/平均延迟)+ otel metrics 浅拷贝。

### 9.3 traceId 贯穿

API 入口生成 `req-{ts}-{rand}`,塞入 `RequestContext`。所有 logger 自动带 traceId,可串联单条 request 的全链路日志。

---

## 10. 部署与运维

### 10.1 构建产物

`npm run build`:
```
dist/
├── index.js           # tsup 单文件 bundle
├── .env               # 部署时编辑
├── AGENTS.md
├── skills/            # 技能目录
├── system-skills/
└── public/
```

部署只需 `dist/`,无需源码 + node_modules。

### 10.2 启动方式

```bash
# 直接运行
node dist/index.js --employee=legal-assistant

# Docker
docker run -d --name ts-multi-agent \
  -p 3000:3000 \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  ts-multi-agent

# Docker Compose
docker compose up -d
```

### 10.3 优雅关闭

`SIGTERM` / `uncaughtException` 触发:
1. `cleanupResources()`: clear TaskQueue + stop sessionContextService + stop skillWatcher + flush L4
2. `process.exit(exitCode)`

---

## 11. 扩展点

新增业务能力应改:

| 需求 | 改什么 |
|---|---|
| 新增技能 | `skills/{name}/SKILL.md` + 重启或热加载 |
| 新增工具 | `src/tools/{name}-tool.ts` + 注册到 `src/tools/tool-registry.ts` |
| 新增数字员工 | `employees/{id}.json` + 启动时 `--employee=<id>` |
| 调整 LLM 链 | `config/llm-fallback.json` |
| 新增 API 端点 | `src/api/index.ts` |
| 新增意图分支 | `src/routers/intent-router.ts` + `handleNonSkillIntent` |
| 新增持久化字段 | `src/types/index.ts` + 对应 store 模块 |
| 调整并发上限 | `src/types/index.ts` 的 `CONFIG` 常量 |
| 调整 SLA | `CONFIG.SLA_*_MS` |
| 新增 Guardrail 规则 | `src/guardrail/rule-engine.ts` |

---

## 12. 已知限制(留待 follow-up)

1. **LLM provider/temperature 按员工 override 未完成** —— `LLMClient` 已支持 options,但 bootstrap 集成仅打 log
2. **进程崩溃在 waiting_user_input 中间** —— LLM 上下文已持久化,但尚未在 `processRequirement` 入口增加"自动检测 waiting task"逻辑(目前靠 session.pendingRequests 间接恢复)
3. **技能 prompt 长度的预算控制** —— SubAgent 当前不限制 system prompt 长度,长 skill body 可能撑爆 context
4. **多进程水平扩展** —— 当前 L1/L3 embedding 缓存是进程内,多进程下不共享

详见 `分析报告.md` 第 4 节。

---

## 13. 已落地:失败重试 + 部分汇总(2026-08-14)

参考 spec `docs/superpowers/specs/2026-08-14-partial-failure-retry-design.md`。

### 13.1 重试循环(TaskQueue)

`src/task-queue/index.ts` 的 `executeTask` 改为带退避的重试循环:

- 默认 `maxRetries=2`(Task 字段,可由 `employee.execution.maxRetries` 覆盖)
- 默认仅重试 LLM 临时错:`TIMEOUT` / `NETWORK_ERROR` / `API_ERROR` 且 `statusCode >= 500`
- 重试退避:base 2s, factor 2, max 60s, ±1s jitter(可通过构造函数 `retryBackoff` 参数测试覆盖)
- 同时识别 `LLMError`(直接)和 `LlmError`(SubAgent `mapSubAgentError` 包出来的)— 后者是生产路径

### 13.2 失败语义(TaskGraphExecutor + MainAgent)

`executeTaskGraph` 区分两种失败模式(spec §7.1):

| 场景 | 行为 |
|---|---|
| **全 task 都 failed** | `throw` SkillError(保留 `AppError` original cause),走原 catch 路径,API 返回 4xx/5xx |
| **部分 failed(有成功)** | `return { success: false, data: { hasPartialFailure: true, failedTasks } }`,MainAgent 走汇总路径 |
| **全 succeeded** | `return { success: true }`(原路径) |

### 13.3 混合状态汇总(ResultAggregator)

`summarizeResults` 重写支持混合 status:

- 拆分 `completedTasks` / `failedTasks` / `waitingTasks`
- LLM 看到全部 task 状态,生成诚实摘要("X 成功, Y 失败")
- **B-1 真正的修法**:rewriter gate 用 `every(completed)` 而非 `taskResults[0]?.status` 或 `taskResults[N-1]?.status`
- **防御纵深**:有 failedTasks 时强制 `completed=false`,即使 LLM 误判
- **Transfer hook**:`TransferToHumanHook = (TaskResult[]) => boolean`,默认 noop,本期未接外部工单系统

### 13.4 Request 状态机扩展

- `Request.status === 'completed'` 语义保持("请求结束")
- `Request.partialFailure?: boolean` 标志区分纯成功 vs 部分失败
- `Request.failedTaskIds?: string[]` 给前端展示
- `SessionStore.completeRequest` 加可选第 5 参数 `{ partialFailure?, failedTaskIds? }`,**`partialFailure=true` 时跳过 `syncRequestStatus` 强制 `status='completed'`**

### 13.5 Feature Flag

`CONFIG.PARTIAL_FAILURE_ENABLED`(默认 `true`):
- `false` 时退退化为旧 throw 路径
- 通过 `process.env.PARTIAL_FAILURE_ENABLED !== 'false'` 解析