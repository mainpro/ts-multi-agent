# LLM API Configuration
# 受支持的 provider:siliconflow / haier(其他已废弃)
# 每个候选在 config/llm-fallback.json 里配置,candidates 的 providerKey 用 <provider>:<modelId>

# SiliconFlow(默认主选)
# Get your key from https://cloud.siliconflow.cn/
SILICONFLOW_API_KEY=

# Haier(备用)
# Get your key from https://modelapi-test.haier.net/model/v1/
HAIER_API_KEY=

LLM_PROVIDER=siliconflow
LLM_MODEL=Pro/MiniMaxAI/MiniMax-M2.5

# LLM_BASE_URL 仅在需要走公司 proxy 时设置,留空则按 provider 默认 endpoint
# (siliconflow: https://api.siliconflow.cn/v1, haier: https://modelapi-test.haier.net/model/v1)
# LLM_BASE_URL=https://your-proxy.internal/v1

# Server port (default: 3000)
PORT=3000
# Temperature 控制模型输出的随机性（0-1之间）
# 较低值（如0.3）使输出更确定、简洁；较高值（如0.8）使输出更随机、有创意
LLM_TEMPERATURE=0.3
# LLM 最大输出 token 数
LLM_MAX_TOKENS=4096